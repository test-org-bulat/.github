name: Build and Run All Tests

on:
  push:
    branches:
      - "*"
  pull_request:

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
        - name: test step
          run: |
            echo "NOOP"